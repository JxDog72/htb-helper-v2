# Real, per-student configuration (may contain a real target IP / student ID).
# Copy config.example.json -> config.json locally; do not commit config.json.
config.json

# Collected research data (session logs, screenshots, notes, evidence).
machines/

# Promo shots live on the `screenshots` git branch for the GitHub README only.
# They are not part of a normal clone or install.
/screenshots/
*.log

# Backup archives created by the helper.
*.zip

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editor/OS cruft
.vscode/
.DS_Store
